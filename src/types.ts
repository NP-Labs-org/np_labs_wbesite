export interface Feature {
  title: string;
  description: string;
  icon: string;
}

export interface UseCaseData {
  title: string;
  subtitle: string;
  description: string;
  features: Feature[];
} 