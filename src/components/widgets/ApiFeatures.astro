---
import { Icon } from 'astro-icon/components';

const apiFeatures = [
  {
    title: 'Suporte Nativo a Múltiplos Modelos de IA',
    description: 'Acesso imediato aos modelos mais avançados da OpenAI, Anthropic, Google, Meta e outros provedores, com seleção automática para melhor custo-benefício.',
    icon: 'tabler:api'
  },
  {
    title: 'Interoperabilidade Total e Capacidades Multimodais',
    description: 'Suporte abrangente para texto, imagens, áudio e vídeo com integração simples via endpoints RESTful.',
    icon: 'tabler:arrows-exchange'
  },
  {
    title: 'Segurança Empresarial e Alta Performance',
    description: 'Foque no desenvolvimento de soluções enquanto a NP Labs gerencia automaticamente autenticação, autorização, cache, balanceamento de carga e logs detalhados de segurança.',
    icon: 'tabler:shield-lock'
  }
];

const codeExample = `# Envie uma mensagem para um assistente de IA
POST /v1/question

# Endpoint
curl -X 'POST' 'https://api.np-labs.org/question'

# Headers
-H 'accept: application/json'
-H 'X-API-Key: api-key' 
-H 'Content-Type: application/json'

# Request Body
-d '{
    "chat_id": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
    "parent_id": 0,
    "question": "string",
    "project_id": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
    "knowledge_bases_ids": [
        "3fa85f64-5717-4562-b3fc-2c963f66afa6"
    ],
    "files_ids": [
        "3fa85f64-5717-4562-b3fc-2c963f66afa6"
    ],
    "agent_id": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
    "stream": false
}'`;
---

<section class="py-16 md:py-20 bg-black text-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6">
    <div class="flex mb-20">
      <div class="text-gray-500">[</div>
      <div class="text-sm font-semibold tracking-wider ml-5 mr-16 text-gray-500">API & DESENVOLVIMENTO</div>
    </div>
    
    <div class="text-3xl font-bold mb-12 max-w-3xl uppercase text-white">
      DESENVOLVA APLICAÇÕES AVANÇADAS COM NOSSA API ROBUSTA
    </div>
    
    <div class="text-gray-400 max-w-3xl mb-16">
      A API da NP Labs oferece aos desenvolvedores acesso programático a todas as funcionalidades de nossa plataforma com documentação completa e exemplos prontos para uso.
    </div>
    
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
      <div class="border border-white/10 p-8 bg-black/30 hover:border-white/30 transition-all duration-300">
        <pre class="font-mono text-sm text-gray-300 overflow-x-auto whitespace-pre-wrap">{codeExample}</pre>
      </div>
      
      <div class="space-y-8">
        {apiFeatures.map((feature) => (
          <div class="flex hover:bg-black/20 p-4 transition-all duration-300">
            <div class="flex-shrink-0 mr-4">
              <div class="w-12 h-12 rounded-full bg-gray-800 flex items-center justify-center">
                <Icon name={feature.icon} class="w-6 h-6 text-gray-400" />
              </div>
            </div>
            <div>
              <h3 class="text-lg font-bold text-white mb-2">{feature.title}</h3>
              <p class="text-gray-400">{feature.description}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section> 