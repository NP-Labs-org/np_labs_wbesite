---
import { Icon } from 'astro-icon/components';
---

<div class="w-full h-full bg-white rounded-lg shadow-lg overflow-hidden">
  <div class="flex flex-col h-full">
    <!-- Header with input -->
    <div class="p-4 border-b border-gray-200">
      <div class="text-lg text-gray-700 mb-4">
        Ask me anything...
      </div>
      <div class="flex items-center gap-2 mb-4">
        <div class="bg-gray-100 rounded-lg py-2 px-3 text-sm text-gray-600">
          Call transcript
        </div>
      </div>
      <div class="relative">
        <input
          type="text"
          readonly
          placeholder="Select a source or add a new one..."
          class="w-full px-4 py-2 bg-gray-50 border border-gray-200 rounded-lg text-sm cursor-not-allowed"
        />
        <button class="absolute right-2 top-1/2 -translate-y-1/2">
          <Icon name="tabler:search" class="w-5 h-5 text-gray-400" />
        </button>
      </div>
      <div class="flex items-center justify-between mt-3">
        <div class="flex items-center gap-2">
          <span class="text-sm text-gray-600">Product Signal from Sales Transcripts</span>
          <Icon name="tabler:chevron-down" class="w-4 h-4 text-gray-400" />
        </div>
        <div class="flex items-center gap-2">
          <div class="relative w-10 h-6 bg-blue-500 rounded-full">
            <div class="absolute right-1 top-1 w-4 h-4 bg-white rounded-full shadow-sm"></div>
          </div>
          <Icon name="tabler:player-play" class="w-5 h-5 text-gray-400" />
        </div>
      </div>
    </div>

    <!-- Grid of cards -->
    <div class="grid grid-cols-2 gap-4 p-4">
      <div class="bg-gray-50 p-4 rounded-lg">
        <div class="flex justify-between items-start mb-2">
          <span class="text-sm font-medium text-gray-700">Company size</span>
          <Icon name="tabler:arrow-up" class="w-4 h-4 text-gray-400" />
        </div>
        <p class="text-xs text-gray-500">
          Classify this company as small (less than 100 employees), medium (100-999 employees), or...
        </p>
      </div>

      <div class="bg-gray-50 p-4 rounded-lg">
        <div class="flex justify-between items-start mb-2">
          <span class="text-sm font-medium text-gray-700">Company description</span>
          <Icon name="tabler:arrow-up" class="w-4 h-4 text-gray-400" />
        </div>
        <p class="text-xs text-gray-500">
          What does this company do? Be specific, but concise
        </p>
      </div>

      <div class="bg-gray-50 p-4 rounded-lg">
        <div class="flex justify-between items-start mb-2">
          <span class="text-sm font-medium text-gray-700">AI experiments</span>
          <Icon name="tabler:arrow-up" class="w-4 h-4 text-gray-400" />
        </div>
        <p class="text-xs text-gray-500">
          What experiments has this company tried with AI? Have they worked well, and if not, why not?
        </p>
      </div>

      <div class="bg-gray-50 p-4 rounded-lg">
        <div class="flex justify-between items-start mb-2">
          <span class="text-sm font-medium text-gray-700">Data lake</span>
          <Icon name="tabler:arrow-up" class="w-4 h-4 text-gray-400" />
        </div>
        <p class="text-xs text-gray-500">
          Does this company have an existing data lake? Did they mention Snowflake, Databricks, or...
        </p>
      </div>
    </div>
  </div>
</div> 